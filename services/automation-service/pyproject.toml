[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "automation-service"
version = "0.11.0"
description = "Business automation engine — durable rules/runs, scheduler, queue ledger, executors, Celery worker/Beat (M10 Step 2)."
requires-python = ">=3.11"
dependencies = [
    "atoz-backend-core==0.12.0",
    "uvicorn[standard]>=0.30,<1",
    "sqlalchemy[asyncio]>=2.0,<3",
    "asyncpg>=0.29,<1",
    "alembic>=1.13,<2",
    "celery>=5.4,<6",
    "redis>=5.0,<6",
    "croniter>=6.0,<7",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3,<10",
    "aiosqlite>=0.20,<1",
    "httpx>=0.27,<1",
    "ruff>=0.6,<1",
    "mypy>=1.10,<2",
    "types-croniter>=6.0,<7",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
