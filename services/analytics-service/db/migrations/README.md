# analytics-service — schema migrations

Alembic environment per the backend-core template
(`libs/backend-core/migrations/`). M3 ships the empty directory; Phase 4
(Database Implementation) adds `env.py`, `alembic.ini`, and the first
revision for this service's tables.
