# apps/api

Business API gateway (FastAPI). M1 foundation: configuration, environment loading,
structured logging, RFC 7807 error handling, request context, and the `/health` endpoint.

No business logic is implemented yet (M1 only).
